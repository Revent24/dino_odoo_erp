<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Tree View with Inline Editing -->
    <record id="dino_uom_view_tree" model="ir.ui.view">
        <field name="name">dino.uom.tree</field>
        <field name="model">dino.uom</field>
        <field name="arch" type="xml">
            <list string="Units of Measure" editable="top" create="true" delete="true">
                <field name="name" required="1"/>
                <field name="conversion_factor"/>
                <field name="related_uom_id" options="{'no_create': True, 'no_open': True}"/>
                <field name="rounding"/>
                <field name="active" widget="boolean_toggle"/>
            </list>
        </field>
    </record>

    <!-- Form View -->
    <record id="dino_uom_view_form" model="ir.ui.view">
        <field name="name">dino.uom.form</field>
        <field name="model">dino.uom</field>
        <field name="arch" type="xml">
            <form string="Unit of Measure">
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="toggle_active" type="object" 
                                class="oe_stat_button" icon="fa-archive">
                            <field name="active" widget="boolean_button" 
                                   options='{"terminology": "archive"}'/>
                        </button>
                    </div>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="rounding"/>
                        </group>
                        <group>
                            <field name="conversion_factor"/>
                            <field name="related_uom_id"/>
                        </group>
                    </group>
                    <group string="Conversion Example" invisible="not related_uom_id">
                        <div class="alert alert-info" role="alert">
                            1 <field name="name" readonly="1" class="oe_inline"/> = 
                            <field name="conversion_factor" readonly="1" class="oe_inline"/> 
                            <field name="related_uom_id" readonly="1" class="oe_inline"/>
                        </div>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Action -->
    <record id="dino_uom_action" model="ir.actions.act_window">
        <field name="name">Units of Measure</field>
        <field name="res_model">dino.uom</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first unit of measure
            </p>
            <p>
                Units of measure are used to specify quantities in documents and inventory.
            </p>
        </field>
    </record>

    <!-- Menu -->
    <menuitem id="menu_dino_uom"
              name="Units of Measure"
              parent="dino_erp.menu_dino_stock_config"
              action="dino_uom_action"
              sequence="10"/>
</odoo>

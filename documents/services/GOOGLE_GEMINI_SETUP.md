# Google Gemini API - Инструкция по настройке

## Получение API ключа

1. Перейдите на [Google AI Studio](https://aistudio.google.com/app/apikey)
2. Войдите с помощью Google аккаунта
3. Нажмите **"Get API Key"** или **"Create API Key"**
4. Скопируйте полученный ключ (формат: `AIzaSy...`)

## Настройка агента в Odoo

### Вариант 1: Через UI (рекомендуется)

1. Откройте **Документы → Настройки → Parser Agents**
2. Найдите агента **"Google Gemini 2.0 Flash"** (он неактивный, показан серым)
3. Откройте его форму (двойной клик)
4. В поле **"API Key"** вставьте ваш ключ
5. Включите агента (переключатель **Active**)
6. Сохраните

### Вариант 2: Через переменные окружения

Добавьте в файл `.env` или системные переменные:

```bash
GOOGLE_GEMINI_API_KEY=AIzaSy_ваш_ключ_здесь
```

## Доступные модели

В Odoo настроены 2 агента для Google Gemini:

### 1. Gemini 2.0 Flash (рекомендуется)
- **Модель**: `gemini-2.0-flash-exp` (можно также `models/gemini-2.0-flash-exp`)
- **Особенности**: Экспериментальная, самая быстрая, бесплатная
- **Лимиты**: 15 запросов/минуту, 1500 запросов/день
- **Стоимость**: Бесплатно (в preview период)
- **Поддержка**: Текст + изображения
- **Качество**: Отличное для документов

### 2. Gemini 1.5 Flash
- **Модель**: `gemini-1.5-flash-latest` (можно также `models/gemini-1.5-flash-latest`)
- **Особенности**: Стабильная версия
- **Лимиты**: 15 запросов/минуту, 1500 запросов/день
- **Стоимость**: Бесплатно до 1500 запросов/день
- **Поддержка**: Текст + изображения
- **Качество**: Стабильное

**ВАЖНО:** Если получаете ошибку 404:
- Попробуйте Model Name: `gemini-2.0-flash-exp` (рекомендуется)
- Или: `gemini-1.5-flash-latest`
- Префикс `models/` добавляется автоматически
- Проверьте список доступных моделей: https://ai.google.dev/gemini-api/docs/models/gemini

## Тестирование

1. Активируйте любого из агентов Gemini
2. Откройте документ (накладная, счет)
3. В поле **"Parser Agent"** выберите **"Google Gemini 2.0 Flash"**
4. Нажмите **"Import Text"** или загрузите изображение
5. Проверьте результат

## Особенности Gemini API

### Преимущества
- ✅ **Бесплатный** доступ (до 1500 запросов/день)
- ✅ **Быстрый** ответ (2-4 секунды)
- ✅ **Multimodal** (текст + изображения)
- ✅ **JSON mode** (структурированный вывод)
- ✅ **Хорошее качество** распознавания украинских документов

### Ограничения
- ⚠️ 15 запросов в минуту (для free tier)
- ⚠️ 1500 запросов в день (для free tier)
- ⚠️ Gemini 2.0 Flash — экспериментальная (может быть нестабильной)

## API Endpoint

Для Google Gemini используется endpoint:

```
https://generativelanguage.googleapis.com/v1beta/models/{model_name}:generateContent
```

Где `{model_name}` может быть:
- `gemini-2.0-flash-exp` (экспериментальная, самая быстрая)
- `gemini-1.5-flash` (стабильная)
- `gemini-1.5-pro` (более мощная, но медленнее)

## Формат запроса

Gemini API использует формат:

```json
{
  "contents": [
    {
      "parts": [
        {"text": "системный промпт"},
        {"text": "текст документа"},
        {"inline_data": {"mime_type": "image/jpeg", "data": "base64..."}}
      ]
    }
  ],
  "generationConfig": {
    "temperature": 0.1,
    "response_mime_type": "application/json"
  }
}
```

## Безопасность

- 🔒 API ключ **НЕ передается** в базу данных Odoo
- 🔒 Хранится в переменных окружения или в зашифрованном виде
- 🔒 Используется только для запросов к Google API

## Поддержка

- [Документация Google AI](https://ai.google.dev/docs)
- [API Reference](https://ai.google.dev/api/python/google/generativeai)
- [Pricing](https://ai.google.dev/pricing)

---

**Последнее обновление**: 2026-01-08

# Google Gemini API - Инструкция по настройке

## Получение API ключа

1. Перейдите на [Google AI Studio](https://aistudio.google.com/app/apikey)
2. Войдите с помощью Google аккаунта
3. Нажмите **"Get API Key"** или **"Create API Key"**
4. Скопируйте полученный ключ (формат: `AIzaSy...`)

## Настройка агента в Odoo

### Вариант 1: Через UI (рекомендуется)

1. Откройте **Документы → Настройки → Parser Agents**
2. Найдите агента **"Google Gemini 2.0 Flash"** (он неактивный, показан серым)
3. Откройте его форму (двойной клик)
4. В поле **"API Key"** вставьте ваш ключ
5. Включите агента (переключатель **Active**)
6. Сохраните

### Вариант 2: Через переменные окружения

Добавьте в файл `.env` или системные переменные:

```bash
GOOGLE_GEMINI_API_KEY=AIzaSy_ваш_ключ_здесь
```

## Доступные модели

В Odoo настроены 2 агента для Google Gemini:

### 1. Gemini 2.0 Flash (рекомендуется)
- **Модель**: `gemini-2.0-flash-exp` 
- **Особенности**: Экспериментальная, самая быстрая, бесплатная
- **Лимиты**: 15 запросов/минуту, 1500 запросов/день
- **Стоимость**: Бесплатно (в preview период)
- **Поддержка**: Текст + изображения
- **Качество**: Отличное для документов
- **Timeout**: 30-90 секунд для текста, 60-120 секунд для изображений

### 2. Gemini 1.5 Flash
- **Модель**: `gemini-1.5-flash-latest`
- **Особенности**: Стабильная версия
- **Лимиты**: 15 запросов/минуту, 1500 запросов/день
- **Стоимость**: Бесплатно до 1500 запросов/день
- **Поддержка**: Текст + изображения
- **Качество**: Стабильное

**ВАЖНО:** 
- Префикс `models/` добавляется автоматически кодом
- В настройках агента указывайте только: `gemini-2.0-flash-exp` или `gemini-1.5-flash-latest`
- НЕ указывайте `models/` в начале - это вызовет ошибку
- Проверьте список доступных моделей: https://ai.google.dev/gemini-api/docs/models/gemini

## Тестирование

1. Активируйте любого из агентов Gemini
2. Откройте документ (накладная, счет)
3. В поле **"Parser Agent"** выберите **"Google Gemini 2.0 Flash"**
4. Нажмите **"Import Text"** или загрузите изображение
5. Проверьте результат

## Особенности Gemini API

### Преимущества
- ✅ **Бесплатный** доступ (до 1500 запросов/день)
- ✅ **Быстрый** ответ (3-10 секунд для текста, 10-30 секунд для изображений)
- ✅ **Multimodal** (текст + изображения)
- ✅ **JSON mode** (структурированный вывод)
- ✅ **Хорошее качество** распознавания украинских документов
- ✅ **Оптимизированные таймауты** (30-120 секунд в зависимости от размера)

### Ограничения
- ⚠️ 15 запросов в минуту (для free tier)
- ⚠️ 1500 запросов в день (для free tier)
- ⚠️ Gemini 2.0 Flash — экспериментальная (может быть нестабильной)

### Что исправлено в последней версии (2026-01-09 v2)

🎯 **По рекомендациям эксперта - полностью переписана логика:**

- 🔧 **УВЕЛИЧЕН базовый таймаут с 30с до 60с** (Gemini может "думать" дольше)
- 🔧 **УПРОЩЕН system_prompt** - убрана длинная обертка, оставлен только шаблон
- 🔧 **УЛУЧШЕНА обработка Markdown** - разрешено модели использовать ```json (естественный стиль)
- 🔧 **ДОБАВЛЕНО поле `barcodes`** в структуру result (для штрих-кодов)
- 🔧 **Шаблон 2.5 KB** (было 16 KB - сокращение в 6.4 раза!)
- 🔧 Таймауты считаются по размеру документа, а не промпта

**Расчет таймаута для текста**: 60с (база) + 3с × (размер_документа / 1000), макс 120с
- Пример: 1,449 символов → 60 + 4 = **64 секунды** (было 34с - НЕ РАБОТАЛО!)
- Пример: 5,000 символов → 60 + 15 = **75 секунд**
- Пример: 30,000 символов → 60 + 60 = **120 секунд** (максимум)

**Почему было зависание?**
1. ❌ Слишком малый таймаут (34с для документа 1.4K - Gemini не успевал "подумать")
2. ❌ Длинный системный промпт с математическими проверками (16 KB!)
3. ❌ Модель пыталась проверить всю математику → зацикливалась

**Что изменилось:**
1. ✅ Таймаут 60-120с (вдвое больше - модель успеет ответить)
2. ✅ Компактный шаблон 2.5 KB (математику делаем в Python, а не в AI)
3. ✅ Разрешено markdown форматирование (снижает когнитивную нагрузку на модель)

## API Endpoint

Для Google Gemini используется endpoint:

```
https://generativelanguage.googleapis.com/v1beta/models/{model_name}:generateContent
```

Где `{model_name}` может быть:
- `gemini-2.0-flash-exp` (экспериментальная, самая быстрая)
- `gemini-1.5-flash` (стабильная)
- `gemini-1.5-pro` (более мощная, но медленнее)

## Формат запроса

Gemini API использует формат:

```json
{
  "contents": [
    {
      "parts": [
        {"text": "системный промпт"},
        {"text": "текст документа"},
        {"inline_data": {"mime_type": "image/jpeg", "data": "base64..."}}
      ]
    }
  ],
  "generationConfig": {
    "temperature": 0.1,
    "response_mime_type": "application/json"
  }
}
```

## Безопасность

- 🔒 API ключ **НЕ передается** в базу данных Odoo
- 🔒 Хранится в переменных окружения или в зашифрованном виде
- 🔒 Используется только для запросов к Google API

## Поддержка

- [Документация Google AI](https://ai.google.dev/docs)
- [API Reference](https://ai.google.dev/api/python/google/generativeai)
- [Pricing](https://ai.google.dev/pricing)

---

**Последнее обновление**: 2026-01-09

## Troubleshooting (Устранение проблем)

### Проблема: Зависание при парсинге
**Симптомы**: Запрос не возвращает ответ, "крутится" загрузка, или таймаут

**Решения**:
1. **Просто попробуйте еще раз** - Google API может быть временно перегружен
2. **Проверьте интернет-соединение** - Gemini требует стабильного соединения
3. **Настройте fallback агента** - система автоматически переключится на резервный
4. **Используйте другую модель**:
   - Groq Llama Vision (быстрее, но хуже качество)
   - OpenRouter Gemini (через прокси, стабильнее)
   - GPT-4o-mini (платный, но очень надежный)
5. **Уменьшите размер данных** - для больших документов (>30,000 символов) разбивайте на части

**Примечание**: Текущие таймауты:
- Текст: 30-90 секунд (зависит от количества символов)
- Изображения: 60-120 секунд (зависит от размера файла)
- Для 13,000 символов таймаут = 69 секунд (должно хватить)

### Проблема: Ошибка 404 Not Found
**Причина**: Неправильное имя модели

**Решение**:
- Используйте `gemini-2.0-flash-exp` (БЕЗ префикса `models/`)
- Или `gemini-1.5-flash-latest`
- Система автоматически добавит `models/` при формировании URL

### Проблема: Ошибка 429 Too Many Requests
**Причина**: Превышен лимит запросов (15/min или 1500/day)

**Решение**:
- Подождите 1 минуту (для RPM лимита)
- Используйте fallback агента с другим API ключом
- Переключитесь на платный план Google AI

### Проблема: Ошибка 401 Unauthorized
**Причина**: Неправильный API ключ

**Решение**:
1. Проверьте API ключ в настройках агента
2. Получите новый ключ на https://aistudio.google.com/app/apikey
3. Убедитесь, что ключ начинается с `AIzaSy`

---

**Последнее обновление**: 2026-01-09
